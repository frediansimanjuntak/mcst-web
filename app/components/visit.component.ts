import { Component } from '@angular/core';
import '../rxjs-operators';

@Component({
  moduleId: module.id,
  selector: 'visit',
  template: `
  	<header></header>
  	<navbar></navbar>
	`,
})

export class VisitComponent  { 
	
}
