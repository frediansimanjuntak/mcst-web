import { Component } from '@angular/core';
import '../rxjs-operators';

@Component({
  moduleId: module.id,
  selector: 'newsletter',
  template: `
  	<header></header>
  	<navbar></navbar>
	`,
})

export class NewsletterComponent  { 
	
}
