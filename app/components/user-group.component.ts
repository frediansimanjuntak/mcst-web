import { Component } from '@angular/core';
import '../rxjs-operators';

@Component({
  moduleId: module.id,
  selector: 'user-group',
  template: `
  	<header></header>
  	<navbar></navbar>
	`,
})

export class UserGroupComponent  { 
	
}
