import { Component } from '@angular/core';
import '../rxjs-operators';

@Component({
  moduleId: module.id,
  selector: 'attachment',
  template: `
  	<header></header>
  	<navbar></navbar>
	`,
})

export class AttachmentComponent  { 
	
}
