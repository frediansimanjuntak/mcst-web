import { Component } from '@angular/core';
import '../rxjs-operators';

@Component({
  moduleId: module.id,
  selector: 'petition',
  template: `
  	<header></header>
  	<navbar></navbar>
	`,
})

export class PetitionComponent  { 
	
}
