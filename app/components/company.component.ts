import { Component } from '@angular/core';
import '../rxjs-operators';

@Component({
  moduleId: module.id,
  selector: 'company',
  template: `
  	<header></header>
  	<navbar></navbar>
	`,
})

export class CompanyComponent  { 
	
}
