import { Component } from '@angular/core';
import '../../rxjs-operators';

@Component({
  moduleId: module.id,
  selector: 'edit-quotation',
  template: ``,
})

export class EditQuotationComponent  { 
	
}
