import { Component } from '@angular/core';
import '../rxjs-operators';

@Component({
  moduleId: module.id,
  selector: 'booking',
  template: `
  	<header></header>
  	<navbar></navbar>
	`,
})

export class BookingComponent  { 
	
}
