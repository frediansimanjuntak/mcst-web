import { Component } from '@angular/core';
import '../rxjs-operators';

@Component({
  moduleId: module.id,
  selector: 'contract',
  template: `
  	<header></header>
  	<navbar></navbar>
	`,
})

export class ContractComponent  { 
	
}
