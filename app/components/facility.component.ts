import { Component } from '@angular/core';
import '../rxjs-operators';

@Component({
  moduleId: module.id,
  selector: 'facility',
  template: `
  	<header></header>
  	<navbar></navbar>
	`,
})

export class FacilityComponent  { 
	
}
