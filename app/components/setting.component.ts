import { Component } from '@angular/core';
import '../rxjs-operators';

@Component({
  moduleId: module.id,
  selector: 'setting',
  template: `
  	<header></header>
  	<navbar></navbar>
	`,
})

export class SettingComponent  { 
	
}
