import { Component } from '@angular/core';
import '../rxjs-operators';

@Component({
  moduleId: module.id,
  selector: 'payment',
  template: `
  	<header></header>
  	<navbar></navbar>
	`,
})

export class PaymentComponent  { 
	
}
