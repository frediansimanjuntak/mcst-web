import { Component } from '@angular/core';
import '../rxjs-operators';

@Component({
  moduleId: module.id,
  selector: 'poll',
  template: `
  	<header></header>
  	<navbar></navbar>
	`,
})

export class PollComponent  { 
	
}
