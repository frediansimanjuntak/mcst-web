import { Component } from '@angular/core';
import '../rxjs-operators';

@Component({
  moduleId: module.id,
  selector: 'user',
  template: `
  	<header></header>
  	<navbar></navbar>
	`,
})

export class UserComponent  { 
	
}
