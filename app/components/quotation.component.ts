import { Component } from '@angular/core';
import '../rxjs-operators';

@Component({
  moduleId: module.id,
  selector: 'quotation',
  template: `
  	<header></header>
  	<navbar></navbar>
	`,
})

export class QuotationComponent  { 
	
}
